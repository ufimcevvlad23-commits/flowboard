# Design QA — календарь и компактные модальные окна

- Source visual truth: `C:\Users\ufimc\AppData\Local\Temp\codex-clipboard-3a148035-e408-4e30-b0f3-f5c260303044.png`
- Implementation screenshot: `C:\Users\ufimc\OneDrive\Документы\Мвркетинг (СЕО)\output\playwright\calendar-viewport-fixed.png`
- Viewport: 509 × 479 px
- State: тёмная тема, карточка задачи открыта, календарь дедлайна задачи открыт

## Full-view comparison evidence

Исходный экран показывает крупный календарь, расположение которого зависит от предполагаемой высоты. В исправленной реализации позиция рассчитывается по фактической высоте календаря и visual viewport: `top 48.2`, `left 21.8`, `right 341.8`, `bottom 469.25` при viewport `510 × 479`. Все четыре границы находятся внутри окна, заголовок карточки и кнопка закрытия также остаются видимыми.

## Focused region comparison evidence

Дополнительный кроп не нужен: оба изображения уже являются сфокусированными снимками календаря. В исправленном состоянии полностью видны заголовок месяца, навигация, семь колонок дней, шесть строк дат и нижние действия «Очистить»/«Сегодня». `scrollHeight` и `clientHeight` календаря равны `419`, поэтому скрытого содержимого и внутренней прокрутки нет.

## Required fidelity surfaces

- Fonts and typography: сохранена существующая типографика Flowboard, иерархия заголовка месяца и подписей дней не изменилась.
- Spacing and layout rhythm: календарь уменьшен до адаптивной ширины 320 px и имеет безопасный отступ от каждой границы viewport; более узкая ширина относительно исходного скриншота является намеренной адаптацией.
- Colors and visual tokens: фон, границы, акцент выбранной даты и состояния навигации используют существующие токены приложения.
- Image quality and asset fidelity: растровые ассеты отсутствуют; используются штатные иконки приложения, качество не деградировало.
- Copy and content: названия месяца, дней недели и действия календаря сохранены.

## Findings

Нет оставшихся P0/P1/P2 несоответствий. На тестовом viewport календарь и родительское модальное окно полностью доступны.

## Comparison history

1. [P1] Исходная логика использовала фиксированную предполагаемую высоту календаря, поэтому при низком окне всплывающее окно могло выходить за viewport.
2. Исправление: добавлено измерение фактической высоты, учёт `visualViewport`, безопасных отступов, `max-height`, прокрутки только как запасного варианта и динамического позиционирования при resize/scroll.
3. Post-fix evidence: календарь целиком находится в границах 510 × 479, нижняя граница `469.25 < 479`, скрытого содержимого нет.

## Implementation checklist

- [x] Календарь измеряется после открытия.
- [x] Положение пересчитывается при resize и scroll.
- [x] Модальные окна ограничены через `100dvh` и safe-area.
- [x] Мобильный footer карточки остаётся доступным.

final result: passed
